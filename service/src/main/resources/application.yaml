spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/innovate_project
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        current_session_context_class: thread
        dialect: org.hibernate.dialect.PostgreSQL10Dialect
